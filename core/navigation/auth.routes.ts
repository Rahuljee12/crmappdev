// Scaffolded for future auth flow.
// Current repo has no auth routes visible under `app/`.

export const authPaths = {
  login: '/auth/login' as const,
  signup: '/auth/signup' as const,
} as const;

